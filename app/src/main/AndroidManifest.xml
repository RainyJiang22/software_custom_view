<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rainy.customview">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Software_custom_view"
        tools:targetApi="31">
        <activity
            android:name=".RouterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".scenery.SceneryActivity"
            android:exported="true" />

        <activity
            android:name=".scratch.ScratchActivity"
            android:exported="true" />

        <activity
            android:name=".slide.SlideActivity"
            android:exported="true" />

        <activity
            android:name=".loading.LoadingActivity"
            android:exported="true" />

        <activity
            android:name=".shine.ShineActivity"
            android:exported="true" />

        <activity
            android:name=".noise.NoiseSplineActivity"
            android:exported="true" />

        <activity
            android:name=".draft.DraftActivity"
            android:exported="true" />
    </application>

</manifest>